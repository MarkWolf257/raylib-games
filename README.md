# Raylib Games
Games made using the raylib library in C.

## Games
- Snake

## Build
Make sure you have the following installed:
- gcc
- cmake
- raylib

*Note:* Install raylib directly into your C compiler.

Follow these steps to build the games.
```bash
cmake -B build
cmake --build build
```
This will build all the games into executables in the build folder.